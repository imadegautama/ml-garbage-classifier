# Dependensi aplikasi STREAMLIT (app.py) — juga default untuk dev lokal.
#
# PENTING: fastapi/uvicorn TIDAK ada di sini. Keduanya memaksa `starlette` versi lama
# yang bentrok dengan Streamlit 1.58 (error: cannot import DEFAULT_EXCLUDED_CONTENT_TYPES).
# Dependensi API ada di requirements-api.txt (di-build sebagai image terpisah).
-r requirements-base.txt
streamlit==1.58.0
