# Dependensi REST API (api.py) — di-build sebagai image TERPISAH dari Streamlit
# (lihat Dockerfile.api). Tidak menyertakan streamlit agar versi `starlette` tidak bentrok.
-r requirements-base.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
